import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Africa x Caribbean Live — The Global Watch Room",
  description:
    "Watch DR Congo's road to the World Cup with fans from around the world. Join the live Discord stadium, choose your country, react in real time. Pay what you feel, starting at $1.",
  openGraph: {
    title: "Africa x Caribbean Live — The Global Watch Room",
    description:
      "One match. One global room. Fans from everywhere. Join the live Discord stadium for FIFA World Cup playoff action.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
